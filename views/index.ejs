<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home - Foodiess Service</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Nunito:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Nunito'; 
            background-image: URL('https://i.pinimg.com/736x/ec/60/5d/ec605dab1eeb47cd3649ca54bc7aeb6c.jpg');
            background-size: cover;       
            display: flex; 
            flex-direction: column;
            justify-content: center;
            align-items: center; 
            min-height: 100vh;
            margin: 0;
            padding: 20px;
            box-sizing: border-box; 
        }
        h1 {
            font-size: 2.5em; 
            font-weight: 700; 
        }
        .btn-personal {
            background-color: #6495ED;
            color: white;
            padding: 12px 28px; 
            border-radius: 25px; 
            font-size: 1.1em; 
            font-weight: 600;
            text-decoration: none;
        }
        .btn-personal:hover {
            background-color: #4169E1;
        }
        .alert {
            font-family: 'Nunito';
            margin-top: 20px; 
            margin-bottom: 20px;
        }
    </style>
</head>
<body>
    <section class="container">
        <div class="row align-items-center justify-content-center g-4"> 
            <div class="col-md-4">
                <span style="font-size: 18em;">&#127828</span>
            </div>
            <div class="col-md-8">
                <h1>Welcome to Foodiess Service!!</h1>
                <% if (messages && messages.length > 0) { %>
                    <div class="alert alert-success" role="alert">
                        <% messages.forEach(function(message) { %>
                            <p class="mb-0"><%= message %></p>
                        <% }); %>
                    </div>
                <% } %>

                <% if (user) { %> 
                    <p class="user-welcome-message">Hello, <%= user.username %></p>
                    <a href="/menu" class="btn btn-personal mt-3 me-2">Menu</a>
                    <a href="/logout" class="btn btn-personal mt-3">Logout</a>
                <% } else { %>
                    <a href="/register" class="btn btn-personal mt-3 me-3">Register</a> 
                    <a href="/login" class="btn btn-personal mt-3">Login</a>
                <% } %>
            </div>
        </div>
    </section>
</body>
</html>